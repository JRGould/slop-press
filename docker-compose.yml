services:
  sloppress:
    build: .
    # Localhost-only binding: never expose beyond the host machine.
    ports:
      - "127.0.0.1:8080:8080"
    env_file:
      - .env
    environment:
      PORT: "8080"
      HOST: "0.0.0.0"
      # Override from the shell via `npm run docker:dev` / `docker:run`.
      # Values here beat whatever `.env` sets for the same key.
      SLOPPRESS_DEV_MODE: "${SLOPPRESS_DEV_MODE:-false}"
    # Read-only root FS. The only writable path is the bind-mounted state dir.
    read_only: true
    tmpfs:
      - /tmp
    volumes:
      - ./state:/app/state
    restart: unless-stopped
    # Minimal capability set.
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
